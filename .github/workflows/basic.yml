name: Basic GitHub Action

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print Message
        run: echo "Hello CJ, GitHub Action is working!"

      - name: Show Files
        run: ls -la
